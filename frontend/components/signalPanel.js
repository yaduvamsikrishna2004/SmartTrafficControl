document.addEventListener('DOMContentLoaded', () => {
  const signalPanel = document.getElementById('signal-panel');
  if (signalPanel) {
    signalPanel.innerHTML = '<p>Signal controls will appear here.</p>';
  }
});
